services:
  postgres:
    image: postgres:16
    container_name: arsa1_postgres
    restart: always
    ports:
      - "5440:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: arsa1_db
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
